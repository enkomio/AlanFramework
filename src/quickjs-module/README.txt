This project implements the JS execution module.

Structure:

* A x86/x64 Windows application that is injected by Alan
* A x86/x64 Console application that is used by the operator to test its script before being injected